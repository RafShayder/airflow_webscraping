SOLUCIÓN RÁPIDA - Ejecuta esto en el servidor:

# Opción 1: Usar sudo (inmediato)
sudo gunzip -c scraper-integratel-docker-image.tar.gz | sudo docker load

# Opción 2: Solución permanente (después de importar)
sudo usermod -aG docker $USER
# Luego cierra sesión y reconecta
exit
ssh daasusr@lnxsrdvmo0011

# Verificar que funcionó
docker images | grep scraper-integratel

# Ahora ya puedes ejecutar sin sudo
docker-compose up -d
